# IBKR News (integration stub)

Your professor says code exists. Put it here and adapt to:
- produce NormalizedItem objects
- push to Redis stream

Tip:
- Wrap synchronous vendor SDK calls into threads (`asyncio.to_thread`) if needed.
